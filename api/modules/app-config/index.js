/**
 * Point of contact for app-config module.
 * @module modules/app-config
 * @example import { routes } from './app-config';
 */

import routes from './app-config-routes';

export default {
  routes,
};
