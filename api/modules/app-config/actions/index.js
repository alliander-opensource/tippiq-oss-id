/**
 * Point of contact for config actions.
 * @module modules/app-config
 * @example import { getAppConfig } from './actions';
 */

import getAppConfig from './get-app-config';

export {
  getAppConfig,
};
