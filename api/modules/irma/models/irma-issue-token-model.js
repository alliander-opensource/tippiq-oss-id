/**
 * IrmaIssueTokenModel.
 * @module modules/irma/models/irma-issue-token-model
 */

import BaseModel from '../../../common/base-model';

const instanceProps = {
  tableName: 'irma_issue_token',
};

const classProps = {};

export default BaseModel.extend(instanceProps, classProps);
